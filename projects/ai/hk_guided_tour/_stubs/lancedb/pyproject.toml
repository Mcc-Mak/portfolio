[project]
name = "lancedb"
version = "0.30.0"
description = "Stub package for lancedb on musllinux (memory storage not used by this project)"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lancedb"]
