[project]
name = "onnxruntime"
version = "1.29.0"
description = "Stub package for onnxruntime on musllinux (RAG embeddings not used by this project)"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["onnxruntime"]
