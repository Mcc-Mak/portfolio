[project]
name = "chromadb"
version = "1.1.1"
description = "Stub package for chromadb on musllinux (RAG/Memory not used by this project)"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["chromadb"]
