# skills/

Everything in this directory is GENERATED by the Orchestrator's Team Architect during
Phase 0 of the entrepreneur-mcp MCP (`commands/entrepreneur-mcp.md`).

Expected generated artifacts:

- `roles-manifest.json` — roles, IDs, towers, file paths
- `sod-matrix.md` — full SoD rules per role
- `tui-modules.md` — question banks for the five mandatory TUI modules
- `role-<id>.md` — one file per synthesised role

Do not hand-edit these files; if the project goal changes, re-run Phase 0 instead.
This placeholder is removed/ignored once generation completes.
