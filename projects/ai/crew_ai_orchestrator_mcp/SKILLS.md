# SKILLS.md

Skills available in this repository. Load one via the skill tool when the task
matches its description (use the `SKILL.md` path below).

## build-crew-ai-mcp

- **Trigger:** "build the MCP server", "implement the crew MCP", "scaffold the
  project", or any session that starts implementing `run_workflow` / the tools
  in `Prompt.md`.
- **What it does:** runs the full build recipe (uv scaffold, MCPServer core,
  YAML crew manager, job state, tests) — the executable form of
  `README.md` `## Quick Start`.
- **Location:** `.opencode/skills/build-crew-ai-mcp/SKILL.md`

Also read `AGENTS.md` (session guidance) and `Prompt.md` (authoritative spec).